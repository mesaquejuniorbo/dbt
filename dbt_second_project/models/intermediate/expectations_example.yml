version: 2

models:
    - name: int_sales
      columns:
        - name: preco_total
          tests:
            - dbt_expectations.expect_column_values_to_be_between:
                min_value: 0
                max_value: 10000
